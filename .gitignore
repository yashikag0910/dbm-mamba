# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Virtual Environment
.venv/
venv/

# Project Dataset files (Ignore large dataset files)
# Anchored to the repo root so this does NOT also ignore the src/data/ package
# (unanchored "data/" matched src/data/, leaving loaders.py / preprocessing.py
# untracked and missing on a fresh clone).
/data/

# OS files
.DS_Store
